stages:
  - build
cache:
  paths:
  - vendor
build:
  stage: build
  tags:
    - docker
  image: simpproject/centos:7-ruby24
  artifacts:
    paths:
      - 'doc'
  script:
          - bundle install --path=/cache
          - bundle update
          - bundle exec rake docs:html

